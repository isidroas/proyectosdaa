# Puerto en el que escucha el socket de la base de datos
puerto: 6000
sensores:
        # Cada uno de estas claves están asociadas con un diccionario {'valor': ..., 'tiempo': ...}
        - sensorLum
        - sensorTemp
